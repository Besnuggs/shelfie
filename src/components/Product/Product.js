import React from 'react'

const Product = (props) => {

    return(
        <div>
            <p>Product</p>
            
            <h1>{props.price} </h1>
            <h1>{props.price}</h1>

        </div>
    )
}

export default Product;